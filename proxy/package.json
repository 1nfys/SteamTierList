{
  "name": "stl-proxy",
  "version": "1.0.0",
  "description": "Proxy for Steam Tier List to Cloudflare Worker",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
